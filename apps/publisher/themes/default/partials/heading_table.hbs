<h2 class="field-title">
    <a data-toggle="collapse" href="#collapse{{name}}" aria-expanded="false" class="collapsing-h2" >
            <i class="cu-btn-exp-col btn-collapsed">{{label}}</i>
    </a>
</h2>
<div class="collapse in" id="collapse{{name}}">
    <table class='table table-bordered'>
        <thead>
            {{#each subheading}}
                <th>{{heading}}</th>
            {{/each}}
        </thead>
        <tbody>
            {{renderHeadingTablePreview .}}
        </tbody>
    </table>
</div>